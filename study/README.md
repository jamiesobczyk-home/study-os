# study/

His progress lives here, one file per course: `<course>.progress.json`.

These files are tracked on purpose — they are his study history, and losing
them resets every review interval to day one. Do not hand-edit them, and do not
commit a cleared file over a real one.

The files are created on the first quiz run. An empty directory here is normal
for a fresh clone.
