# Node / editor noise
node_modules/
.DS_Store
*.log

# Recovered progress files written when a progress file could not be parsed.
# The real progress files in progress/ are tracked on purpose — they are his
# study history, and losing them resets every review interval.
*.progress.json.broken-*

# Video build output — regenerate with video/build/scenes.mjs
video/out/

# Stray screenshots from browser test runs
/q-*.png
